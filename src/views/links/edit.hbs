<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/links/edit/{{link.id}}" method="POST">
                        <div class="form-group">
                            <input type="text" value="{{link.titulo}}" name="titulo" id="" placeholder="Titulo" class="form-control" autofocus>
                        </div>
                        <div class="form-group">
                            <input type="url" value="{{link.url}}" name="url" id="" class="form-control" placeholder="Url">
                        </div>
                        <div class="form-group">
                            <textarea name="descripcion"  id="" rows="2" class="form-control"
                                placeholder="Descripción">{{link.descripcion}}</textarea>
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>